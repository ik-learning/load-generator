[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = ">=6.2.0"
responses = ">=0.13.3"
freezegun = ">=1.1.0"
mock = ">=4.0.3"
parametrized = "v0.8.1"

[packages]
requests= ">=2.25.0"
numpy = "*"
jsonschema = ">=3.2.0"
asyncio = ">=3.4.3"
aiohttp = ">=3.7.4"

[scripts]

[requires]
python_version = "3.8"
